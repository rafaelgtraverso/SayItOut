import React from 'react';
import { View } from 'react-native';
import s from '../css/styles';
import { Text } from 'react-native-elements';

const AccountScreen = () => {
    return (
        <View>
            <Text style={s.text}> AccountScreen </Text>
        </View>
    );
};

export default AccountScreen;